server.port=9090

# evn setting
spring.profiles.active=test

# data base config

mybatis.config-location=classpath:mybatis-config.xml
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.s2u2m.mindfly.account.entity